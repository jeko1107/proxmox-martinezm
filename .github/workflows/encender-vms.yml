# .github/workflows/encender-vms.yml
name: Encender VMs Proxmox (7:00 AM y 3:30 PM)

on:
  schedule:
    - cron: '0 5 * * *'   # 7:00 AM España → 5:00 UTC
    - cron: '30 13 * * *' # 3:30 PM España → 13:30 UTC
  workflow_dispatch:      # También puedes ejecutarlo manualmente

jobs:
  encender:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Instalar OpenVPN
        run: |
          sudo apt update -qq
          sudo apt install -y openvpn

      - name: Crear archivos OpenVPN
        run: |
          echo "$OPENVPN_CONFIG" > /tmp/instituto.ovpn
          echo "$OPENVPN_AUTH" > /tmp/auth.txt
          echo "$OPENVPN_CACERT" > /tmp/cacert.pem
          chmod 600 /tmp/instituto.ovpn /tmp/auth.txt /tmp/cacert.pem
        env:
          OPENVPN_CONFIG: ${{ secrets.OPENVPN_CONFIG }}
          OPENVPN_AUTH: ${{ secrets.OPENVPN_AUTH }}
          OPENVPN_CACERT: ${{ secrets.OPENVPN_CACERT }}

      - name: Ejecutar script
        run: python start_vms_openvpn.py
        env:
          PROXMOX_IP: ${{ secrets.PROXMOX_IP }}
          PROXMOX_TOKEN: ${{ secrets.PROXMOX_TOKEN }}

      - name: Limpiar
        if: always()
        run: |
          rm -f /tmp/instituto.ovpn /tmp/auth.txt /tmp/cacert.pem
          pkill openvpn || true

      - name: Mostrar hora actual
        run: |
          echo "Ej
